apiVersion: v1
kind: Pod
metadata:
  name: log-writer
  namespace: default
spec:
  containers:
    - name: log-writer-container
      image: busybox
      command:
        - "sh"
        - "-c"
        - "while true; do echo $(date) >> /var/log/app/app.log; sleep 10; done"
      volumes:
    - name: log-writer-storage
      persistentVolumeClaim:
        claimName: log-writer-claim
  volumeMounts:
        - mountPath: "/var/log/app"
          name: log-writer-storage

